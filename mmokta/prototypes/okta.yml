url: https://github.com/fvigo/minemeld-okta
description: >
  MineMeld Extension for OKta

prototypes:
  user_output:
    author: Francesco Vigo - Palo Alto Networks
    class: mmokta.OktaOutput
    development_status: EXPERIMENTAL
    node_type: output
    indicator_types:
      - any
    tags:
      - extension
      - okta
      - user-id
      - remediation
    description: >
      MineMeld Extension for Okta, implementing a single Output Node for the user-id remediation use case. Parameters can be configured through the WebUI. Domain mapping is still not functional.

    config:
      # Domain Map
      domain_map: your_domain_map


