url: https://github.com/fvigo/minemeld-okta
description: >
  Okta Module for MineMeld

prototypes:
  user_output:
    author: Francesco Vigo - Palo Alto Networks
    class: mmokta.OktaOutput
    development_status: EXPERIMENTAL
    node_type: output
    indicator_types:
      - any
    tags:
      - extension
      - okta
      - user-id
      - remediation
    description: >
      Output Node for Okta for user remediation use case. Parameters can be configured through the WebUI. Domain_Map is still not fully implemented.

    config:
      # Domain Map
      domain_map: your_domain_map


